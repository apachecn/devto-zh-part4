# 你最大的错别字诱因是什么？

> 原文：<https://dev.to/voidjuneau/what-is-your-worst-typo-trigger-37e3>

我刚开始学习 React，我一直输入`status`而不是`state`。

```
// App.js with React
state = {something: 'whatever'} 
```

Enter fullscreen mode Exit fullscreen mode

vs

```
$ git status 
```

Enter fullscreen mode Exit fullscreen mode

我在同时学习 Python 和 JavaScript 的时候也很难对付`self` vs `this`，还有`a.length` vs `len(a)`。听说用 R 的人也在经历类似的事情。

你的呢？