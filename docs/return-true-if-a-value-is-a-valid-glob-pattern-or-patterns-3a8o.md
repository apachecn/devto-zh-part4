# 如果值是一个或多个有效的全局模式，则返回 true。

> 原文：<https://dev.to/samandar/return-true-if-a-value-is-a-valid-glob-pattern-or-patterns-3a8o>

```
from functorflow import f

r = f('glob', 'foo/*.js')
print(r) # True 
```

Enter fullscreen mode Exit fullscreen mode

如果值是一个或多个有效的全局模式，则返回 true。

[https://repl.it/@functorflow/glob?lite=true](https://repl.it/@functorflow/glob?lite=true)