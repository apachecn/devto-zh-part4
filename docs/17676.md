# PHP 决策/条件语句

> 原文：<https://dev.to/mikoyoarts/php-decision-making-conditional-statements-41c4>

作为人类，我们经常面临这样或那样的决定。我们做出决策的结果旨在解决我们遇到的挑战。在 PHP 中没有任何区别，决策也可以由 PHP 编程语言来处理，从而使得甚至在没有人类帮助的情况下实现特定的过程成为可能。

决策是基于选择的。每一个选择都附有一个下划线过程，每当选择该选项时都应执行该过程。下面我们将讨论和解释 PHP 编程语言中一些不同形式的决策。慢慢地跟着我，你会得到一路上的每一点信息。

PHP 条件语句的类型

1.  如果一个条件为真，if 语句将执行一些代码。语法 if (condition) {条件为真时要执行的代码；}

例子<？PHP
$ t = date(" H ")；

if ($t < "20") {
echo“祝您愉快！”；
}
？>

1.  如果...else 语句 if else 语句用于在条件为真时执行某个代码，在条件为假时执行另一个代码。语法 if (condition) {条件为真时要执行的代码；} else {条件为假时要执行的代码；}

例
<？PHP
$ x = 10；
$ y = 20；
if($ x>= $ y){
echo $ x；
} else {
echo $ y；
}
？>

1.  如果...埃尔塞夫...else 语句使用 if...埃尔塞夫...如果第一个条件为 false，则使用 else 语句指定要测试的新条件。语法 if (condition) {条件为真时要执行的代码；} elseif (condition) {第一个条件为假且该条件为真时要执行的代码；} else {所有条件都为假时要执行的代码；}

例
<？PHP
$ age = 21；
如果($年龄< =13) {
附和“孩子”；
} else if($ age>13&T14】$ age<19){
echo《少年》；
} else {
呼应“大人”；
}
？>

1.  嵌套 if 语句
    嵌套 if 语句意味着在另一个 if 块中有一个 if 块，即在另一个控制语句中有一个控制语句。
    语法
    if(条件 1)
    {
    if(条件 2) {
    该条件为真时要执行的代码；如果此条件为真，则执行 else {
    代码；
    }
    } else {
    if(条件 2)
    {
    此条件为真时要执行的代码；
    } else {
    此条件为真时要执行的代码；
    }
    }

2.  Switch Case 语句
    Switch 语句是 if-elseif-else 语句的替代语句。使用 switch 语句选择要执行的多个代码块之一。
    语法
    switch(n){
    case value 1:
    //n = value 1
    break 时要执行的代码；
    case value 2:
    //n = value 2
    中断时要执行的代码；
    ...
    默认:
    //n 不同于所有标签时执行的代码

例
$ today = ' Tue '；

switch($ Today){
case " Mon ":
echo "今天是星期一。"；
突围；
案例“星期二”:
回声“今天是星期二。”；
破；
case "Wed":
echo "今天是星期三。"；
破；
案例“Thu”:
echo“今天是星期四。”；
破；
案例“Fri”:
echo“今天是星期五。”；
破；
案例“星期六”:
echo“今天是星期六。”；
破；
案例“孙”:
呼应“今天是星期天。”；
破；
默认:
回显“无效日”；
}

1.  跳转语句有时你可能想让循环无条件地开始，并让括号内的语句决定何时退出循环。有两种特殊的语句可以在循环中使用:Break 和 Continue。

*   Break 语句:结束当前结构的执行。Break 接受一个可选的数字参数，告诉它要中断多少次嵌套结构的执行。

例
<？PHP
$ x = 0；
while($ x)
{
echo($ x .)；
if($ x = = 5)
{
break；
}
$ x++；
}
//输出 0 1 2 3 4 5

？>

*   Continue 语句:用于停止处理循环中的当前代码块，并转到下一次迭代。Continue 语句和 Break 可以带一个可选的数字参数，告诉它应该跳到多少级封闭循环的末尾。

例
<？PHP
for($ I = 0；$ I<5；$ i++)
{

if($ x = = 2)
{
继续；
}
echo $ I . "；
}
//输出 0 1 3 4
？>

*   Exit 语句:exit 语句用于终止当前的执行流。一旦找到 exit 语句，它将终止程序。

例
<？PHP
$ a = 5；
$ b = 5.0；
if($ a = = $ b)
{
//使用 exit()
exit('变量相等')终止带有消息的脚本；
}
else
{
//使用 exit()
echo“变量不相等”的消息终止脚本；
}
？>