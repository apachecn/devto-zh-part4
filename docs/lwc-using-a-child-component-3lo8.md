# LWC——使用子组件

> 原文：<https://dev.to/brettmn/lwc-using-a-child-component-3lo8>

[https://www.youtube.com/embed/A3GxlsZGXjE](https://www.youtube.com/embed/A3GxlsZGXjE)

嗨，我是 WIPDeveloper.com 的布雷特，我想快速了解一下如何使用子组件和自定义的 Lightning web 组件。

所以我们要做的第一件事就是做一个组件来容纳子组件。所以我要做一个新的 lightning web 组件。

我要称之为家长。然后我会做一个新的。我们要做的第一件事是给它一些标记。给它一个父代的头衔。所以我们知道我们在哪里，所以我们可以在页面上看到它。我拼错了 parent，所以让我们改变一下，或者让我拼一下 component。

所以我要部署这个，它没有合适的目标。因此

首先，我要从另一个人那里拿走目标，这样我就不用这么做了。

部署它。

现在我们必须添加一个旅游页面。所以刷新我们的组件列表。

把它放在那里。我们暂时看到，这是一个子组件，这是因为我之前做了一次模拟。现在我们刷新页面，我们应该看到我们的父组件。

现在，我们需要添加一个子组件。因此，让我们转到添加一个名为 child 的新 Lightning web 组件。

我们将把它标记为 exposed，创建它，给它一些 HTML。我打算在小范围内进行一次彻底检查。

现在，它被部署到我们的 org，但是我们必须得到它，以便它显示在父组件的主体中。因此，我们需要在父组件中做的是，像添加普通 HTML 元素一样添加它。

因此，所有定制 web 组件都要求在标记中用破折号分隔两个名称或两个单词。因为我们使用的是 Lightning Web 组件，所以我们所有的标签都以名称空间为前缀。所以我们有一个名字的子组件实际上有一个名称空间的前缀。所以这种情况下是`c`，然后是`c-child`。

当我们再次刷新页面时，我们已经将子组件添加到父组件中。

我们会看到我们的孩子，当然这看起来不太好。为什么看起来不太好？

哦，因为它在卡片外面。

哎呀。

不，它会在卡里。一切都会好的。

现在是我们了

看起来不太好，这孩子没垫够。

不对。哎呀。错误的自动完成。

`slds-p-around_small`。

刷新页面。

那里的填充物稍微好一点。我们为什么不做那个媒介

现在我们可以看到孩子和父母站成一排。

给你。我们现在在父组件中使用子组件。

## 现在就这样。

记得报名参加 **[每周一次的单口相声！你可以得到我们关于 WIPDeveloper.com 的任何最新信息。](https://wipdeveloper.com/newsletter/)**

使用子组件的帖子[首先出现在 WIPDeveloper.com](https://wipdeveloper.com/lwc-using-a-child-component/)[的](https://wipdeveloper.com)上。